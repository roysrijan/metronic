"use strict";

// Class definition
var KTLandingPage = function () {
    // Private methods
    var initPortfolio = function(product) {
        
    }

    // Public methods
    return {
        init: function () {
            
        }   
    }
}();

// Webpack support
if (typeof module !== 'undefined') {
    module.exports = KTLandingPage;
}

// On document ready
KTUtil.onDOMContentLoaded(function() {
    KTLandingPage.init();
});
